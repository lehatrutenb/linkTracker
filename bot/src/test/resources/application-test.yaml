app:
  telegram:
    link:
      tracker:
        url: ${wiremock.server.baseUrl}/bot/
        init-state-try-set-fixed-rate: 1s
        token: test-token
        update-listener-sleep: 1s
        debug: true
